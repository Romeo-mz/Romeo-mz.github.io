---
phase: 3.5-artistic-3d-portfolio-experience
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
autonomous: true
requirements:
  - PORT3D-02

must_haves:
  truths:
    - "User sees animated constellation particles floating across portfolio background"
    - "Particles drift with medium energy creating depth and movement"
    - "Lines draw from nearby particles to cursor position as mouse moves"
    - "Particle system runs smoothly at 60fps on desktop"
  artifacts:
    - path: "index.html"
      provides: "Canvas element for particle rendering, particle system class and animation loop"
      contains: "canvas id=\"particles-canvas\""
      min_lines: 100
  key_links:
    - from: "Particle.update()"
      to: "requestAnimationFrame loop"
      via: "delta time calculation"
      pattern: "particle\\.update\\(deltaTime\\)"
    - from: "mousemove listener"
      to: "Particle.connectToMouse()"
      via: "mouse position tracking"
      pattern: "mouse\\.x.*mouse\\.y"
---

<objective>
Create animated constellation particle background with cursor interaction for portfolio page.

Purpose: Establish depth and visual interest with floating particles that connect to cursor, creating an artistic interactive background layer that enhances the 3D portfolio experience without distracting from content.

Output: Canvas-based particle system with 50-80 star-like particles, medium drift animation, cursor-proximity line connections, running at 60fps.
</objective>

<execution_context>
@/home/martinr7/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/martinr7/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/3.5-artistic-3d-portfolio-experience/3.5-CONTEXT.md
@.planning/phases/3.5-artistic-3d-portfolio-experience/3.5-RESEARCH.md
@index.html
@style.css
</context>

<tasks>

<task type="auto">
  <name>Create canvas particle system with constellation aesthetic</name>
  <files>index.html</files>
  <action>
Add canvas element as first child of body (before .page-container) for background rendering:
- Canvas positioned fixed, full viewport, z-index -1 (behind all content)
- ID "particles-canvas" for script reference
- Set canvas width/height accounting for devicePixelRatio for crisp rendering on Retina displays

Implement particle system in inline script block before closing body tag:
- Particle class with properties: x, y, vx, vy (velocity), radius (1-3px for star-like points)
- Constructor: randomize position across canvas, set velocity range -0.5 to 0.5 for medium energy drift
- update(deltaTime) method: increment position by velocity × deltaTime × 0.06, wrap around edges
- draw() method: arc with rgba(255, 255, 255, 0.8) fill for constellation star aesthetic
- connectToMouse() method: calculate distance to mouse, if < 150px draw line with opacity fading by distance

Create 60 particles (desktop default per user decision: 50-80 balanced presence)

Canvas setup:
- Get 2d context
- Scale by devicePixelRatio for sharp rendering
- Handle window resize to maintain full viewport coverage

Per RESEARCH.md particle system code example lines 440-519, use requestAnimationFrame with delta time for frame-rate independent animation.
  </action>
  <verify>
Open index.html in browser, observe floating white particles drifting across background at medium speed. Particles should wrap around screen edges and feel like constellation stars in space.
  </verify>
  <done>
60 particles visible as white dots (1-3px radius) floating with medium energy drift across portfolio background. Particles wrap around viewport edges creating continuous motion.
  </done>
</task>

<task type="auto">
  <name>Implement cursor interaction with proximity line connections</name>
  <files>index.html</files>
  <action>
Add mouse tracking to canvas element:
- mousemove event listener captures clientX/clientY
- Convert to canvas-relative coordinates using getBoundingClientRect()
- Store in module-level mouse object { x: 0, y: 0 }

Extend animation loop to call particle.connectToMouse() for each particle:
- Already implemented in Particle class connectToMouse() method from Task 1
- Lines drawn from particle to cursor when distance < 150px
- Line opacity: rgba(255, 255, 255, 1 - dist/150) for distance-based fade
- Line width: 0.5px for subtle constellation aesthetic

Animation loop structure (per RESEARCH.md lines 503-518):
- Track lastTime for delta calculation
- Clear canvas each frame with clearRect()
- Loop particles: update(deltaTime), draw(), connectToMouse()
- requestAnimationFrame(animate) for continuous loop

Initialize loop on page load with requestAnimationFrame(animate) call after particle creation.

Per user decision from CONTEXT.md: "Lines connect particles to mouse cursor (not auto-connect between particles)" - do NOT implement particle-to-particle connections.
  </action>
  <verify>
Move mouse over portfolio page. Lines should dynamically draw from nearby particles (within ~150px) to cursor position. Lines fade with distance and disappear beyond threshold. No console errors. Animation runs smoothly without jank.
  </verify>
  <done>
Mouse movement creates dynamic line connections from nearby particles to cursor. Lines fade opacity based on distance. Cursor interaction creates engaging constellation network effect without performance degradation.
  </done>
</task>

</tasks>

<verification>
**Visual checks:**
- [ ] Canvas fills entire viewport behind content (z-index -1 working)
- [ ] 60 particles visible as white star-like dots
- [ ] Particles drift with noticeable medium-energy movement
- [ ] Particles wrap around edges (no disappearing at boundaries)
- [ ] Mouse movement draws lines from nearby particles to cursor
- [ ] Lines fade opacity with distance (not uniform)
- [ ] No particle-to-particle auto-connections (only cursor connections)

**Performance checks:**
- [ ] Animation runs at ~60fps (check DevTools Performance monitor)
- [ ] No dropped frames during mouse movement
- [ ] Canvas renders crisply on Retina displays (devicePixelRatio handled)

**Code checks:**
- [ ] Canvas positioned fixed with z-index -1
- [ ] Particle class has update(), draw(), connectToMouse() methods
- [ ] Animation loop uses requestAnimationFrame with delta time
- [ ] Mouse coordinates convert to canvas-relative properly
</verification>

<success_criteria>
Portfolio background displays animated constellation particle system with 60 floating star-like dots drifting at medium energy. Mouse movement dynamically connects nearby particles with fading lines. Animation runs smoothly at 60fps with no jank. Particles wrap around viewport edges for continuous effect. Canvas renders behind all content without interfering with portfolio card or navigation.
</success_criteria>

<output>
After completion, create `.planning/phases/3.5-artistic-3d-portfolio-experience/3.5-01-SUMMARY.md`
</output>
